<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    public function run(): void
    {
        // Intentionally empty: the first administrator is created from the
        // one-time initialization screen, not from a predictable demo account.
    }
}
